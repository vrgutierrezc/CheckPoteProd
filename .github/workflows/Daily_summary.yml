# .github/workflows/daily_summary.yml
name: Daily Site-Check Summary

on:
  schedule:
    # Run every day at 4:30 PM Bogotá time (UTC-5) → 21:30 UTC
    - cron: '30 21 * * *'
  workflow_dispatch:      # allow manual trigger

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Send daily heartbeat email
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ secrets.SMTP_SERVER }}
          server_port:    ${{ secrets.SMTP_PORT }}
          username:       ${{ secrets.SMTP_USERNAME }}
          password:       ${{ secrets.SMTP_PASSWORD }}
          subject:        "🕒 Daily Site Check – No changes"
          body:           |
            Hola,

            Este es tu correo diario de confirmación: no se detectaron cambios en la sección
            monitorizada del sitio del Ministerio de Minas y Energía en las últimas 24 horas.

            Si hubiera habido cambios, ya habrías recibido una alerta.

            Saludos.
          to:             ${{ secrets.NOTIFY_EMAIL_EMAIL }}
          from:           ${{ secrets.FROM_EMAIL }}
