name: Daily Site-Check Heartbeat

on:
  schedule:
    # Run daily at 4:30 pm Bogotá (UTC–5 → 21:30 UTC)
    - cron: '30 21 * * *'
  workflow_dispatch:

jobs:
  heartbeat:
    runs-on: ubuntu-latest

    steps:
      - name: Send daily heartbeat
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ secrets.SMTP_SERVER }}
          server_port:    ${{ secrets.SMTP_PORT }}
          username:       ${{ secrets.SMTP_USERNAME }}
          password:       ${{ secrets.SMTP_PASSWORD }}
          subject:        "🕒 Daily Heartbeat – No changes"
          body:           |
            Hola,

            Este es el correo diario de confirmación: no se han detectado cambios
            en el párrafo monitorizado del sitio del Ministerio de Minas y Energía.

            Saludos.
          from:           ${{ secrets.FROM_EMAIL }}
          to:             ${{ secrets.NOTIFY_EMAIL_EMAIL }}
          bcc:            ${{ secrets.BCC_EMAILS }}
          from:           ${{ secrets.FROM_EMAIL }}
